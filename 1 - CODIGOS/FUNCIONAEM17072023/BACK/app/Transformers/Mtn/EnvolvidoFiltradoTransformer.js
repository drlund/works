'use strict'

const BumblebeeTransformer = use('Bumblebee/Transformer')

/**
 * EnvolvidoFiltradoTransformer class
 *
 * @class EnvolvidoFiltradoTransformer
 * @constructor
 */
class EnvolvidoFiltradoTransformer extends BumblebeeTransformer {
  /**
   * This method is used to transform the data.
   */
  transform (model) {
    return {
     // add your transformation object here
    }
  }
}

module.exports = EnvolvidoFiltradoTransformer
